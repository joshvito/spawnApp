'use strict';

module.exports = function (ngModule) {
  //require('./parameter.dir')(ngModule);
  require('./aquaticsResource.ctrl')(ngModule);
  require('./about')(ngModule);
  //require('./main')(ngModule);


};